The :ref:`AI protocol manager <config_http_filters_ai_protocol_manager>` filter, when installed as an
upstream filter, rewrites each attempt for the model route plan target it connected to: the path,
authority, credential header and request body ``model``.
